import { SearchRecordPipe } from './search-record.pipe';

describe('SearchRecordPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRecordPipe();
    expect(pipe).toBeTruthy();
  });
});
